import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.cekareasatpam',
  appName: 'Cek Area Satpam',
  webDir: 'dist'
};

export default config;
